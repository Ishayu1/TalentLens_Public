"""
TalentLens configuration — paths, model settings, and UI constants.
"""

from pathlib import Path

# ---------------------------------------------------------------------------
# Paths (relative to the *project root*, i.e. TalentLens_Public/)
# ---------------------------------------------------------------------------
PROJECT_ROOT = Path(__file__).resolve().parent.parent.parent

DATA_DIR = PROJECT_ROOT / "data"
DS3_DIR = DATA_DIR / "ds3"
MEMBERS_CSV = DS3_DIR / "member_resumes" / "members.csv"
MEMBER_RESUMES_DIR = DS3_DIR / "member_resumes"
BOARD_RESUMES_DIR = DS3_DIR / "board_resumes"
BOARD_RESUMES_TXT = DS3_DIR / "board_resumes" / "board.txt"

# Pipeline output artifacts (generated by pipeline/03_faiss_indexing.ipynb)
FAISS_INDEX_PATH = DATA_DIR / "artifacts" / "resume_index.faiss"
METADATA_PATH = DATA_DIR / "artifacts" / "member_resumes_metadata.json"
CHUNK_METADATA_PATH = DATA_DIR / "artifacts" / "member_chunks_metadata.json"
CONFIG_JSON_PATH = DATA_DIR / "configs" / "config.json"

# ---------------------------------------------------------------------------
# Embedding model
# ---------------------------------------------------------------------------
MODEL_NAME = "all-MiniLM-L6-v2"
EMBEDDING_DIM = 384

# ---------------------------------------------------------------------------
# Search defaults & reranker config
# ---------------------------------------------------------------------------
DEFAULT_TOP_K = 20
MIN_SCORE_THRESHOLD = 0.0

# Cross-encoder reranker (optional, loaded lazily in SearchEngine)
RERANKER_ENABLED = True  # flip to True once a model is trained and saved
RERANKER_MODEL_PATH = PROJECT_ROOT / "models" / "talentlens-cross-encoder-sft-v1"

# ---------------------------------------------------------------------------
# Predefined skill suggestions (shown in the sidebar & autocomplete)
# ---------------------------------------------------------------------------
SKILL_SUGGESTIONS = [
    "Python",
    "Machine Learning",
    "Data Science",
    "NLP",
    "SQL",
    "React",
    "Computer Vision",
    "Java",
    "C++",
    "JavaScript",
    "TypeScript",
    "TensorFlow",
    "PyTorch",
    "Deep Learning",
    "Statistics",
    "R",
    "AWS",
    "Docker",
    "Kubernetes",
    "Node.js",
    "HTML/CSS",
    "Git",
    "MongoDB",
    "PostgreSQL",
    "Tableau",
    "Power BI",
    "Spark",
    "Hadoop",
    "Scikit-learn",
    "Pandas",
    "NumPy",
    "LLMs",
    "RAG",
    "Transformers",
    "BERT",
    "GPT",
    "Flask",
    "Django",
    "FastAPI",
    "Swift",
    "Kotlin",
    "Rust",
    "Go",
    "Scala",
]

# ---------------------------------------------------------------------------
# UI colour palette (matches the dark-theme mockup)
# ---------------------------------------------------------------------------
COLORS = {
    "bg_dark": "#1a1f2e",
    "sidebar_bg": "#141824",
    "card_bg": "#ffffff",
    "accent_orange": "#f97316",
    "accent_teal": "#2dd4bf",
    "text_primary": "#ffffff",
    "text_secondary": "#94a3b8",
    "text_dark": "#1e293b",
    "border": "#334155",
    "rank_blue": "#38bdf8",
    "score_green": "#22c55e",
    "score_yellow": "#eab308",
    "score_red": "#ef4444",
}
